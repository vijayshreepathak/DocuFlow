FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY ./src ./src
COPY ./logs ./logs

ENV PYTHONPATH=/app/src

# Entrypoint is set by docker-compose (worker, beat, or flower) 